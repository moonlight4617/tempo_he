$('.messages').append("<li class='message'><%= @message.content %><br><div class='sender'><% if session[:user_id]%><%= User.find(session[:user_id]).name %><% elsif session[:owner_id]%><%= Shop.find(params[:id]).name %><% end %><%= @message.created_at.strftime('%m/%d %H:%M')%></div></li>");
$('.js-form')[0].reset();
$('.js-message-errors').empty();