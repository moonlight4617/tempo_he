<!----

12~15時まで予約
@start_time [12,13,14,22,23]

def mold_time
s = start_time.min

while @start_time.include(s) do
@start_time.include(s)を満たす時のみ繰り返し処理を行う。
if @start_time.include(s)
@end_time = s + 1
@start_time.slice(s)
end
s += 1
end

出てきた@end_timeとsを使ってresevationを1つ作成、保存。
end


sを1つずつ足して行って、sが配列@start_timeに含まれない値になった時、処理を終了。
もし、@start_timeに値が残っている場合は再度処理実行。

-->